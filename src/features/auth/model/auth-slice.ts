import { createAppSlice } from "@/common/utils"

export const authSlice = createAppSlice({
  name: "auth",
  initialState: {
    isLoggedIn: false
  },
  selectors: {

  },
  reducers: () => ({

  })
})